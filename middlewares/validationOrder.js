function validationOrder(request, response, next) {
    const {
        guest_email,
        guest_name,
        guest_surname,
        city,
        country,
        total_amount,
        items
    } = req.body;


}